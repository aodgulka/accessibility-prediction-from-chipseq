# accessibility-prediction-from-chipseq
This repo contains code to predict accessibility changes at cis-regulatory elements based on ChIP-seq features. The pipeline implements a random forest classifier to predict binary accessibility changes, as well as ridge regression to model quantitative changes in accessibility.
